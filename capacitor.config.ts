
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pretub.store',
  appName: 'pretub.store',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};


export default config;
